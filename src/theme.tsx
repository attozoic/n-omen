import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#0abf40'
    },
    secondary: {
      main: '#19857b'
    },
    background: {
      default: '#fff'
    }
  }
});

export default theme;
